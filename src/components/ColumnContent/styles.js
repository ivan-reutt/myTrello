import styled from 'styled-components';

const StyledTaskListWrap = styled.div`
  padding: 2px;
  width: 100%;
  max-height: 500px;
  overflow-y: auto;
  overflow-x: visible;
`;

export default StyledTaskListWrap;
